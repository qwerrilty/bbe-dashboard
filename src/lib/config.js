// ============================================
// Feature flags
// Flip these to true when the feature is ready
// ============================================

export const FEATURES = {
  // Set to true when Gmail sending is wired up (see lib/email.js)
  EMAIL_ENABLED: false,

  // Set to true when Google Calendar invite sending is ready
  CALENDAR_INVITES_ENABLED: false,

  // Set to true when Gmail inbox sync is ready
  GMAIL_SYNC_ENABLED: false,
}